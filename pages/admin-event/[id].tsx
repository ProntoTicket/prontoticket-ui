'use client';

import React, { useEffect, useState } from 'react';
import Header from '@/components/ui/header';

const ECommerce: React.FC = () => {
  const BASE_URL = 'http://localhost:5110';

  return <>LOCAL EVENT ADMIN PAGE</>;
};

export default ECommerce;
